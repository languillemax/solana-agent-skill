import test from 'node:test';
import assert from 'node:assert';

test('Squads Multisig - Création de compte multisig', () => {
  assert.ok(true);
});

test('Solana Blinks - Génération de lien Blink', () => {
  assert.ok(true);
});
